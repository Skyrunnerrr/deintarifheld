# ADR-011 — Ops API Boundary

STATUS=ACCEPTED
TRANCHE=DTH-M9  
DATE=2026-08-13  

## Context
Phase-4 dual-plane reality (root public LIVE; packages Ops LOCAL) frozen at M8C `4545c7b`. Architecture must support secure Ops, durable workflows, and future agents without big-bang rewrite of working public intake.

## Problem
Ops API Boundary must be decided before implementation so later AuthZ/RLS/workflow work does not accidentally entrench the wrong topology.

## Constraints
- No microservices without need (P20)
- Public intake must not depend on CC/AI (availability)
- RLS ≠ protection from service-role (M8C)
- In-memory controls not production-suitable
- Implementation not authorized in M9

## Options considered
See DTH-M9 evidence report scorecard (Options A/B/C).

## Decision
Ops API is the sole human/worker/agent entry to Ops domain: AuthN + AuthZ + domain services. Exposes BFF for CC and tool endpoints for agents/workers with capability checks.

## Why
Single policy enforcement point.

## Rejected options
Multiple ad-hoc APIs per package; CC bypassing API.

## Security impact
Central deny-by-default.

## Privacy impact
Purpose-bound endpoints.

## Operations impact
Must be highly available for Ops (not for public intake).

## Migration impact
Evolve from packages/ops-api.

## Rollback
Architecture decision can be revised before implementation starts; after impl, follow ADR-specific rollback in implementation tranche.

## Dependencies
M8C baseline; M10 for data/SOT details; M6 does not block this ADR.

## Open questions
Listed in DTH-M9.md where verification still required.

## Evidence references
docs/autonomous-os/evidence-reports/DTH-M8.md, DTH-M8C.md, DTH-M9.md; repo inventory (root app/api, packages/*).


## M9R HARDENING

STATUS note after M9R:
CANONICAL_DOMAIN_SERVICE_BOUNDARY=shared domain services with policy-equivalent logic.
Transport:
- CC → Ops API → Domain Service
- Agent → Tool Gateway → Ops API → Domain Service
- Worker → Domain Service directly (trusted backend; same AuthZ/audit/final-control; no mandatory HTTP hairpin).
