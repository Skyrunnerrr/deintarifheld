# A11-14 Audit / Evidence

Successful operator writes insert `public.audit_events` (`a11.command.*`) plus `ops.operator_commands`. Audit view is read-only and paginated. Evidence chains remain in A7–A10 canonical rows (evaluation → offer hash → switch payload hash → confirmed switch → lifecycle). No chain-of-thought / AI reasoning panel.
