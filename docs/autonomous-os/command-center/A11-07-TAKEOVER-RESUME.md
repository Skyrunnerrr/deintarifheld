# A11-07 Takeover / Resume

Granularity: A1 workflow (`activateTakeover` / `resumeWorkflowControl`). A11 `TAKEOVER_CASE` applies to non-terminal workflows for `workflow_instances.case_id`.

Not per-domain takeover. Not a UI flag. Stale `CONTROL_VERSION` → `STALE_OPERATOR_VIEW`. Resume does not manufacture jobs.
