# A6-16 Test Evidence

Suite: `npm run test:dth:a6` → 25/25 PASS (local disposable Postgres `127.0.0.1:55432/dth_a1`).

Covers: schema, magic/fake extension, size, hash/dup, cross-case isolation, electricity/gas extraction, German 50.000 kWh, form conflict, OCR_REQUIRED, prompt injection, path traversal, global kill receipt, takeover, A7 evidence, no tariff, A4 handoff mock, reprocess fingerprint, anon grants 0, DATA_IMPORT kill, owner flags.
