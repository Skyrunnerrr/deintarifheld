# A7-11 Multi Supply Point

`supply_point_count` from lead `standorte` (min 1). When component `per_supply_point=true`, fixed/one-time amounts multiply by supply point count (e.g. 2× annual base fee).

