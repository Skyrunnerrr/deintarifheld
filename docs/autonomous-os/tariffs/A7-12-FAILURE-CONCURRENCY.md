# A7-12 Failure & Concurrency

Evaluation fingerprint = case + profile fingerprint + catalogue snapshot hash + policy versions. Unique fingerprint + single `is_current` per case. Duplicate runs reuse. Profile/catalogue change → new evaluation; prior marked not current for A8.

