# A7-14 A6 Integration

Consumes only `getCaseEnergyEvidence(pool, caseId)`. No PDF parsing in A7. Conflicts / ambiguous consumption block pricing (`INPUT_CONFLICT` / HUMAN_REVIEW). A6 OWNER_* unchanged.

