# A7-15 A8 Handoff Contract

`getCurrentTariffEvaluation(caseId)` + `isTariffEvaluationCurrent(evaluationId)`.

`nextCapability=OFFER_PREPARE` (constant only — A8 not implemented). Stale profile/catalogue → `offerReady=false`. Explicit: `offerGenerated=false`, `supplierSwitchInitiated=false`.

