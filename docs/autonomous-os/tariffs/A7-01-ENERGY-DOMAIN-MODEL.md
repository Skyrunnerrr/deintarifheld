# A7-01 Energy Domain Model

Canonical entities: suppliers → products → versions → price components + eligibility rules; catalogue snapshots; case energy profiles; evaluations + results.

Energy types align with A3 `CanonicalEnergyType` (ELECTRICITY|GAS). Customer segments BUSINESS|PRIVATE. Currency ISO-3. Price basis NET|GROSS.

