# A13-04 Campaign Versioning

Material changes (budget, destination, CTA, content, channel) create a new revision. Prior approvals become stale. Unique `(campaign_id, revision_no)` and one-current index.
