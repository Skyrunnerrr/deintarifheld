# A13-06 Destination Tracking

Server-side allowlist hosts: `deintarifheld.de`, `www.deintarifheld.de`. Paths: `/unternehmen`, `/faq`, `/ablauf`. Opaque `acq_ref` is non-PII. Client campaign IDs are not authority.
