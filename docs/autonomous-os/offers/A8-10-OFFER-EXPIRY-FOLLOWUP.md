# A8-10 Expiry and follow-up

Durable `OFFER_EXPIRE` at `valid_until`. `OFFER_FOLLOWUP_DUE` max 1, delay 50ms synthetic. Cancelled on accept/reject/expiry. `OWNER_OFFER_FOLLOWUP_POLICY_REQUIRED=true` — E2 schedule is not production cadence.
