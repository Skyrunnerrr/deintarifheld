# A8-13 A7 integration

Inputs: `getCurrentTariffEvaluation`, `isTariffEvaluationCurrent`. Capability `OFFER_PREPARE` (same string as A7 handoff). A8 must not import `calculateTariffCost` / eligibility / ranking. A6/A7 OWNER_* flags remain true.
