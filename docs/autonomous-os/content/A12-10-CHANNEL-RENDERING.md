# A12-10 Channel Rendering

Canonical facts stay in the revision. Channel adapters format only (escape + synthetic length caps).

E2 channels: `SYNTHETIC_LINKEDIN`, `SYNTHETIC_BLOG`. Limits are test/config data, not remembered live platform values.

`channelFactsEquivalent` compares headline/body facts. Claim rewrite per channel is forbidden. Links remain server-allowlisted domains.
