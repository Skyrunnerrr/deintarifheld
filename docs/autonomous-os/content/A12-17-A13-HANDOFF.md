# A12-17 A13 Handoff

`buildA13ContentHandoff` exposes published content only:

content_id, publication_id, channel, published_at, CTA, destination (`https://www.deintarifheld.de/unternehmen`), tracking_correlation_id (`a12:{item}:{pub}`), purpose, performance snapshots.

`acquisitionActionsPerformed=0`. No paid campaign, no lead mutation. UTM not introduced as an invasive stack; correlation id is server-generated and bounded.
