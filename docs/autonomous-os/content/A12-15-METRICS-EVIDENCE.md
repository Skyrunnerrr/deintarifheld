# A12-15 Metrics Evidence

`getPostMetrics` via test adapter. Snapshots append-only (`ops.content_metric_snapshots`). Fields: impressions, clicks, engagement. `revenueAttribution` always null.

Do not claim a post generated a customer. `UNPROVEN_CONTENT_REVENUE_ATTRIBUTIONS=0`.

No autonomous strategy mutation from one post.
