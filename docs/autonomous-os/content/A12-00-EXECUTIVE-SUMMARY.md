# A12-00 Executive Summary

A12 is the governed content-production loop for DeinTarifheld. It is not a prompt-to-post generator.

Canonical path:

strategy → structured brief → candidate generation → claim/brand/risk validation → approval → durable schedule → publication intent → test publisher → readback → metric snapshot → A13 handoff shape.

AI output is a candidate, never approval, never truth, never a publication. Unsupported savings, synthetic-tariff-as-live, fake testimonials, market superlatives, unapproved links, and prompt-injection authority are blocked. Production auto-publish is not claimed.

Kill mapping: `KillDomain.AUTOMATION_ENGINE` (no ninth domain). E2 uses `DeterministicTestContentGenerator` and `DeterministicTestPublishingProvider`. `LIVE_AI=0`, `LIVE_SOCIAL=0`.

`A12_RESULT=CLOSED_E2_LOCAL_CONTENT_PROVIDERS_STAGING_PENDING`

Staging/production content providers, social accounts, brand/claim production policy, and auto-publish remain Owner decisions. Staging and production autonomy stay **NO**.
