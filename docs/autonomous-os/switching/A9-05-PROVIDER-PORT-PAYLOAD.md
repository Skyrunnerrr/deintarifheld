# A9-05 Provider port and payload

Port: validateSubmission / submitSwitch / getSubmission / getSwitchStatus / cancelSubmission. E2: `TEST_SWITCH_V1` in-memory, no network. Payload `A9SwitchPayloadV1` hashed; no IBAN/SEPA. Client cannot choose provider or payload.
