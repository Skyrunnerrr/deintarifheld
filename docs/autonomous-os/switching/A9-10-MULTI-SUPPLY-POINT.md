# A9-10 Multi-supply-point

`supply_point_count` copied from accepted scope (`PRESERVE_SCOPE`). E2 does not collapse locations. `OWNER_MULTI_SUPPLY_SWITCH_POLICY_REQUIRED=true` (ALL_OR_NOTHING vs per-point unresolved).
