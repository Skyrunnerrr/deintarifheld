# Purpose Boundaries

business_energy → eligible  
private_energy → rejected by A2 atomic acceptor  
career → separate endpoint; never sales case  
bot/invalid → no handoff (existing public route behavior unchanged)
