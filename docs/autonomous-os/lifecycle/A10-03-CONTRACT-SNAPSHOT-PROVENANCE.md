# A10-03 Contract snapshot provenance

Snapshot stores start (A9), optional expected end / notice / guarantee with `provenance` JSON. Missing A7 terms → `term_incomplete`, no invented dates. TEST_LIFECYCLE_POLICY_V1 synthetic terms only when `allowSyntheticTerms`.
