# A10-01 Customer lifecycle domain

`ops.customer_lifecycles` (unique confirmed `switch_attempt_id`; current unique per case+energy+supply_point_count) → contract snapshots → events → renewal cycles (one current) → provider events. Case status is not contract state.
