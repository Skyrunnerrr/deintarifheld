# A10-05 Pre-active vs active

confirmed_supply_start > asOf → PRE_ACTIVE + LIFECYCLE_ACTIVATE_DUE. At execution: switch still CONFIRMED, fingerprint, provider not cancelled. Stale activation=0. Start ≤ asOf → ACTIVE.
