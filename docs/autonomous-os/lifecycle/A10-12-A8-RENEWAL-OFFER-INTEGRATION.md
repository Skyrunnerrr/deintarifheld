# A10-12 A8 renewal offer integration

prepareOffer with `offerPurpose: RENEWAL` allowed even if prior revision ACCEPTED (historical ACCEPTED state preserved; new current revision). Snapshot `offer_purpose`. No live send in A10 tests.
