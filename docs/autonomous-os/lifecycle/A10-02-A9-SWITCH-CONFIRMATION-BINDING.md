# A10-02 A9 switch confirmation binding

Lifecycle created only from `ops.lifecycle_handoffs` + attempt state CONFIRMED + confirmed_supply_start. Duplicate attempt → reuse. Unconfirmed/rejected switch cannot activate.
