# A0 Command Center Target

## Current

LOCAL / DEV / READ ONLY — inbox/cases/tasks views (`packages/cc`).

## Target (A11)

Overview (leads/cases/appointments/offers/switching/renewals), Autonomy (jobs/agent runs/kill), Exceptions (approvals/ambiguity/DLQ/provider fail), Controls (pause/resume/takeover/kill), KPIs including `FOUNDER_INTERVENTIONS_PER_CASE`.

A0 does not implement UI.
