# A3-05 Revision and Provenance

Each material fingerprint change increments revision. Same fingerprint → reuse current assessment.  
Observations carry `source_kind`. Normalized facts store statuses/values without duplicating email/phone.
