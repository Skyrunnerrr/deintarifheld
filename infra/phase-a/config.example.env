# Copy to infra/phase-a/config.env (gitignored, chmod 0600). Never commit real values.

# === EXTERNAL (required for apply) ===
SUPABASE_ACCESS_TOKEN=
RESEND_API_KEY=
CONTROLLED_TEST_EMAIL=

# Checkdomain API: NOT required (owner decision — no paid API)
# DNS automation skipped; mail uses LEADS_MAIL_MODE=mock / onboarding@resend.dev

# === OPTIONAL / AUTO ===
SUPABASE_ORG_ID=
SUPABASE_DB_PASSWORD=
SUPABASE_PROJECT_NAME=deintarifheld-phase-a
SUPABASE_REGION=eu-central-1

NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

LEADS_FROM_EMAIL=DeinTarifheld <onboarding@resend.dev>
LEADS_TO_EMAIL=
LEADS_MAIL_MODE=mock

VERCEL_TOKEN=
VERCEL_PROJECT_NAME=deintarifheld-leads-api
PREVIEW_URL=

CRON_SECRET=
LEADS_ADMIN_SECRET=
LEADS_RATE_LIMIT_SALT=
LEADS_ALLOWED_ORIGINS=https://deintarifheld.de,https://www.deintarifheld.de,http://localhost:3000
